<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.IO.Compression.FileSystem" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Strict-pin to [10.0.0] — the .NET 10 GA floor. Every VS 2026 patch
      ships 10.0.0.x with x >= 0, so compiling against the floor is
      binary-safe across the whole VS 2026 lineup. AssemblyResolveBootstrap
      (in ForgePilot.VSExtension) substitutes whatever's on disk at runtime.
      See that file and the host csproj for the full rationale.
    -->
    <PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="[10.0.0]" />
    <PackageReference Include="System.Threading.Channels" Version="[10.0.0]" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="[10.0.0]" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="[10.0.0]" />
    <PackageReference Include="Microsoft.Extensions.Options" Version="[10.0.0]" />
    <!--
      Pinned to 4.2.0 — ForgePilot.VSExtension transitively pulls in
      Serilog.Extensions.Logging 10.0.0 and Serilog.Sinks.File 7.0.0, both
      of which were built against exactly Serilog 4.2.0.0. Strict
      strong-name binding in .NET Framework rejects any other patch.
    -->
    <PackageReference Include="Serilog" Version="4.2.0" />
    <PackageReference Include="System.Text.Json" Version="[10.0.0]" />
    <PackageReference Include="PolySharp" Version="1.15.0" PrivateAssets="all" />
  </ItemGroup>

  <!--
    Reference the MCP permission helper exe so it gets built before us. The
    .exe is not linked as a managed reference; it's embedded as a resource
    (see EmbedMcpHelper target below) and extracted at runtime by
    ClaudeCliProcessHost.ResolveHelperExePath. Packaging it this way means
    the VSIX can never lose it.
  -->
  <ItemGroup>
    <ProjectReference Include="..\ForgePilot.Services.McpPermissionServer\ForgePilot.Services.McpPermissionServer.csproj">
      <ReferenceOutputAssembly>false</ReferenceOutputAssembly>
      <Private>false</Private>
      <SkipGetTargetFrameworkProperties>true</SkipGetTargetFrameworkProperties>
    </ProjectReference>
  </ItemGroup>

  <!--
    Zip the entire MCP helper bin folder (exe + its net472 dependencies like
    System.Text.Json, Microsoft.Bcl.AsyncInterfaces, etc.) into a single
    embedded resource. ClaudeCliProcessHost.ResolveHelperExePath extracts
    the zip at runtime into a versioned %LOCALAPPDATA% folder so the exe
    can resolve its deps from the same directory. The zip is staged under
    obj so it doesn't leak into OutputPath.
  -->
  <Target Name="EmbedMcpHelper"
          BeforeTargets="PrepareForBuild"
          DependsOnTargets="ResolveProjectReferences">
    <PropertyGroup>
      <_McpHelperSourceDir>$(MSBuildThisFileDirectory)..\ForgePilot.Services.McpPermissionServer\bin\$(Configuration)\net472\</_McpHelperSourceDir>
      <_McpHelperZipPath>$(IntermediateOutputPath)ForgePilot-mcp-permissions.zip</_McpHelperZipPath>
    </PropertyGroup>
    <Error Condition="!Exists('$(_McpHelperSourceDir)ForgePilot-mcp-permissions.exe')"
           Text="MCP helper exe not found at $(_McpHelperSourceDir). Build ForgePilot.Services.McpPermissionServer first." />
    <MakeDir Directories="$(IntermediateOutputPath)" />
    <Delete Files="$(_McpHelperZipPath)" />
    <ZipDirectory SourceDirectory="$(_McpHelperSourceDir)"
                  DestinationFile="$(_McpHelperZipPath)" />
    <ItemGroup>
      <EmbeddedResource Include="$(_McpHelperZipPath)">
        <LogicalName>ForgePilot-mcp-permissions.zip</LogicalName>
      </EmbeddedResource>
    </ItemGroup>
  </Target>

</Project>
